general:   
    .xpm defines the sizes
    .png defines the graphics
    
if there is no inactive, active and inactive states show the same graphic



title-1-active.png = defines graphic for "behind the app icon on the top left"
              .xpm = defines the height for ...
              
title-2-active.png = defines the part between the app icon and the title3 

title-3-active.png = defines the long part above the "window title"         

title-4-active.png = defines the small part between title3 (long part with the window title) and the buttons on the right.

title-5-active.png = Part BEHIND the bottons on the right